<%= form_with(model:[vision, comment], local: false) do |f| %>
  <%= f.text_field :comment, placeholder: "コメント", class: "rounded-pill comment-textarea" %>
  <%= f.submit "送信する", class: "btn btn-base-1 mt-20 pull-right " %>
<% end %>