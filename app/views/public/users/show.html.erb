<h2>投稿</h2>
<%= link_to "投稿", new_vision_path %>
<table>
<% @visions.each do |vision| %>
<tr>
  <td><%= link_to vision.title, vision_path(vision.id) %></td>
  <td><%= vision.finish_status %><td>
</tr>
<% end %>
</table>

