<nav class ="navbar navbar-expand-sm bg-dark text-white mb-3">
  <a class="navbar-brand" herf="#"><span><i class="fab fa-airbnb"></i></span></a>
  <div class="col ml-auto">
    <div class="row text-right">
      <div class="col ml-auto">
        <button class="navbar-toggler bg-dark" type="button" date-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-expanded="false" aria-label="Toggle navigation   bg-right">
          <span class="navbar-toggler-icon "></span>
        </button>
      </div>
      <div class="collapse navbar-collapse" id="navbarNavDropdown" >
        <ul class="navbar-nav ml-auto">
          <% if admin_signed_in? %>
            <li class="mx-2"><%= link_to '投稿一覧',  admin_visions_path %></li>
            <li class="mx-2"><%= link_to '会員一覧',  admin_users_path %></li>
            <li class="mx-2"><%= link_to 'ジャンル一覧',  admin_genres_path %></li>
            <li class="mx-2 sm-ml-2"><%= link_to 'ログアウト', destroy_admin_session_path, method: :delete %></li>
          <% elsif user_signed_in? %>
            <li class="mx-2"><%= link_to 'マイページ', user_path(current_user.id) %></li>
            <li class="mx-2"><%= link_to '投稿一覧', visions_path %></li>
            <li class="mx-2"><%= link_to '投稿', new_vision_path %></li>
            <li class="mx-2 sm-ml-2"><%= link_to 'ログアウト', destroy_user_session_path, method: :delete %></li>
          <% end %>
        </ul>
      </div>
    </div>
     <div class="row my-1">
      <div class="col text-right">
        <%= render 'public/searchs/search' %>
      </div>
    </div>
  </div>
</nav>
